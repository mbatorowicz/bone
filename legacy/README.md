# Legacy Bone (v0.1)

Poprzednia monolityczna implementacja. Trzymana jako referencja algorytmów
(Verlet/kNN, CUDA forces, stare mapowanie cech).

Uruchomienie (jeśli potrzeba):

```bash
$env:PYTHONPATH="legacy"
python -c "from bone.studio import main; main()"
```

Nowa aplikacja: pakiet `src/bone` (v0.2+).
